<%= form_for(@project) do |f| %>
    <%= f.hidden_field :user_id %>
    <%= f.label :name %>
    <%= f.text_field :name %><br>

    <%= f.label :language %>
    <%= f.text_field :language %><br>

    <%= f.label :description %>
    <%= f.text_field :description %><br>

    <%= f.label :url %>
    <%= f.text_field :url %><br>

    <%# if current_user.provider == "github" %>
        <%# select("project", "imported", options_for_select(@github_projects.collect {|proj| [proj[:name], proj[:github_id]] }), { include_blank: true }) %>
    <%# end %>

    <%= f.submit %>
<% end %>
